#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/wheeljack/Downloads/redemption-os/node_modules/.pnpm/vite@6.3.5_@types+node@25.9.2_jiti@2.7.0_lightningcss@1.30.1/node_modules/esbuild/bin/esbuild
