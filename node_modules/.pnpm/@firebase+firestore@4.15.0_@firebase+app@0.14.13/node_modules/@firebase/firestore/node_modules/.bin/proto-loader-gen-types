#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../@grpc/proto-loader/build/bin/proto-loader-gen-types.js" "$@"
else
  exec node  "$basedir/../../../../@grpc/proto-loader/build/bin/proto-loader-gen-types.js" "$@"
fi
# cmd-shim-target=/home/wheeljack/Downloads/redemption-os/node_modules/.pnpm/@firebase+firestore@4.15.0_@firebase+app@0.14.13/node_modules/@grpc/proto-loader/build/bin/proto-loader-gen-types.js
